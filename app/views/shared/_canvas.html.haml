#fixed-top{}
  %audio#music{ src: Rails.configuration.settings['filehost'] + song.filename, preload: 'auto' }
  #progress
  #controls.actions{ data: {id: song.id, filename: song.filename, name: song.name} }
    %a.download{href: download_song_path(song.id), alt: 'Download Song', title: 'Download'} &#8623;
    %span.btn.add{data: {id: song.id} } &#43;
    %span.btn.play.inactive &#9658;
    %span.btn.pause &#9616;&#9616;

    %span.name=song.name
    %marquee#ticker{scrolldelay: 5, scrollamount: 2}
      =song.description
      %a{href: song.posturl, target: '_blank'}
        &nbsp; (read more)


    -if @playlist
      %span#playlist-count
        =@playlist.count
      =render partial: 'shared/playlist', locals: { playlist: @playlist }
    #opts
      %input{type: 'radio', name: 'visual-option', value: 'bar'} Bar
      %input{type: 'radio', name: 'visual-option', value: 'line'} Line


  %canvas#viz{ width: 1350, height: 100}
