%span#playlist-toggle{ ng: { click: 'togglePlaylist()' } }
  {{playlist.length}}

#download-list.section{ ng: { click: 'changeActiveList(playlist)'}}
  %h4
    %span#playlist-count
      {{playlist.length}}
    Songs for
    %a.download-list{ href: download_zip_path(Time.new.seconds_until_end_of_day.to_s), title: 'Download Playlist', target: '_blank'} download

  =render partial: 'cart'